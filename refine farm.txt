local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")
local VirtualInputManager = game:GetService("VirtualInputManager")

local LocalPlayer = Players.LocalPlayer
local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
local HRP = Character:WaitForChild("HumanoidRootPart")
local Handler = Character:WaitForChild("PlayerHandler"):WaitForChild("HandlerEvent")
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

-- === CONFIG ===
local questName = "Find Diamond?"
local questPath = function()
	return PlayerGui.Main.QuestAlertFrame.QuestGUI:FindFirstChild(questName)
end

local farmingSpots = {
	{ center = Vector3.new(-1199, 2, -1340), radius = 35 },
	{ center = Vector3.new(-1060, 2, -1420), radius = 35 },
}

-- === UTILITIES ===
local function fireDialogue(choice)
	local args = {{ Choice = choice }}
	ReplicatedStorage.Remote.Event.Dialogue:FireServer(unpack(args))
end

local function m1Attack(pos)
	local args = {{
		CombatAction = true,
		LightAttack = true,
		MouseData = CFrame.new(pos)
	}}
	Handler:FireServer(unpack(args))
end

local function useSkill(pos)
	local args = {{
		Skill = true,
		AttackType = "Down",
		Key = "E",
		MouseData = CFrame.new(pos)
	}}
	Handler:FireServer(unpack(args))
end

local function clickQuestButton()
	local btn = PlayerGui:WaitForChild("Main"):WaitForChild("FunctionFrame"):WaitForChild("QuestButton")
	if btn and btn.AbsolutePosition then
		local pos = btn.AbsolutePosition + (btn.AbsoluteSize / 2)
		VirtualInputManager:SendMouseButtonEvent(pos.X, pos.Y, 0, true, game, 0)
		task.wait()
		VirtualInputManager:SendMouseButtonEvent(pos.X, pos.Y, 0, false, game, 0)
	end
end

local function tpTo(position)
	LocalPlayer.Character:MoveTo(position)
end

local function pressKey(key)
	VirtualInputManager:SendKeyEvent(true, key, false, game)
	VirtualInputManager:SendKeyEvent(false, key, false, game)
end

-- === CRAFTING ===
local function checkAndCraft()
	pressKey(Enum.KeyCode.BackSlash)
	task.wait(0.2)
	for _ = 1, 4 do pressKey(Enum.KeyCode.Down) task.wait(0.1) end
	pressKey(Enum.KeyCode.Return)
	task.wait(1.7)

	local inv = PlayerGui.Main.BackpackMainFrame.Inventory.ItemScroll
	local ember = inv:FindFirstChild("Chunk of Ore")
	local rawText = ember and ember:FindFirstChild("Qty") and ember.Qty.Text or ""
	local qty = tonumber(string.match(rawText, ">(%d+)<"))
	print("[CRAFT DEBUG] Parsed Qty:", qty)

	task.wait(0.5)
	pressKey(Enum.KeyCode.Return)
	task.wait(0.5)
	pressKey(Enum.KeyCode.BackSlash)
	task.wait(0.2)

	if qty and qty > 50 then
		tpTo(Vector3.new(-860, 26, -25))
		task.wait(3)
		fireclickdetector(Workspace.NPC.UniversalCrafting.ClickDetector)
		task.wait(1)
		fireDialogue("[ Craft ]")
		task.wait(1)

		for i = 1, 10 do
			for _, item in ipairs({ "Blue Fragment", "Blue Sappyre", "Red Fragment", "Red Emperor" }) do
				local args = { "Special", item }
				ReplicatedStorage.Remote.Event.CraftingRemote:FireServer(unpack(args))
				task.wait(0.25)
			end
		end

		pressKey(Enum.KeyCode.BackSlash)
		task.wait(0.2)
		for _ = 1, 2 do pressKey(Enum.KeyCode.Down) task.wait(0.1) end
		for _ = 1, 5 do pressKey(Enum.KeyCode.Left) task.wait(0.1) end
		pressKey(Enum.KeyCode.Return)
		task.wait(0.3)
		pressKey(Enum.KeyCode.BackSlash)
	end
end

-- === QUEST ===
local function startQuest()
	tpTo(Vector3.new(-1237, 2, -1500))
	task.wait(1)
	fireclickdetector(Workspace.NPC.LeeTheMiner.ClickDetector)
	task.wait(1)
	fireDialogue("[ Accept Quest ]")
	task.wait(0.5)
	fireDialogue("Exit = true")
end

local function completeQuest()
	tpTo(Vector3.new(-1237, 2, -1500))
	task.wait(1)
	fireclickdetector(Workspace.NPC.LeeTheMiner.ClickDetector)
	task.wait(1)
	fireDialogue("Yes, I've done it.")
	task.wait(0.5)
	fireDialogue("Exit = true")
end

local function waitForQuestText()
	clickQuestButton()
	repeat task.wait(0.5)
	until questPath() and questPath().Text:match("%(.-/.-%)") or questPath().Text:find("Quest Completed")
end

local function isQuestComplete()
	local q = questPath()
	return q and q.Text and q.Text:find("Quest Completed")
end

-- === MOB FARMING ===
local function getNearbyTargets(centerPos, radius, npcName)
	local targets = {}
	for _, npc in ipairs(Workspace.Lives:GetChildren()) do
		if npc.Name == npcName and npc:FindFirstChild("HumanoidRootPart") and npc:FindFirstChild("Humanoid") then
			local hrp = npc.HumanoidRootPart
			if (hrp.Position - centerPos).Magnitude <= radius and npc.Humanoid.Health > 0 then
				table.insert(targets, npc)
			end
		end
	end
	return targets
end

local function attackNPC(npc)
	local hrp = npc:FindFirstChild("HumanoidRootPart")
	if hrp and npc:FindFirstChild("Humanoid") and npc.Humanoid.Health > 0 then
		repeat
			Character:MoveTo(hrp.Position + Vector3.new(1, 0, 0))
			m1Attack(hrp.Position)
			useSkill(hrp.Position)
			task.wait(0.25)
		until not npc:FindFirstChild("Humanoid") or npc.Humanoid.Health <= 0
	end
end

local function farmSpot(centerPos, radius, npcName)
	local targets = getNearbyTargets(centerPos, radius, npcName)
	for _, npc in ipairs(targets) do attackNPC(npc) end

	tpTo(centerPos)
	task.wait(2)

	local retryTargets = getNearbyTargets(centerPos, radius, npcName)
	for _, npc in ipairs(retryTargets) do attackNPC(npc) end
end

-- === MAIN LOOP ===
while true do
	checkAndCraft()
	startQuest()
	waitForQuestText()

	while not isQuestComplete() do
		for _, spot in ipairs(farmingSpots) do
			farmSpot(spot.center, spot.radius, "Miner Goon Lv.50")
		end
		clickQuestButton()
		task.wait(1)
	end

	completeQuest()
end
